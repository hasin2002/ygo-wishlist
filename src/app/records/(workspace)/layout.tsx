import { RecordsWorkspace } from "@/components/records/records-workspace";

export default function RecordsWorkspaceLayout({ children }: { children: React.ReactNode }) {
  return <RecordsWorkspace>{children}</RecordsWorkspace>;
}
